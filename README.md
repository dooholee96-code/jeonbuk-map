# 전북 학교·농촌유학 지도

전북특별자치도 **14개 시군 742개 초·중·고**와 **2026학년도 2학기 농촌유학 운영·희망학교**를
한 지도에서 봅니다. 기본 화면은 전북 전체이고, 선택창에서 시·군을 고르면 그 지역만 봅니다.

**API 키가 필요 없습니다.** 지도는 Leaflet + OpenStreetMap 을 쓰고 Leaflet 은 저장소에 동봉돼
있어, 정적 호스팅에 파일만 올리면 그대로 뜹니다. 인수인계할 때 계정이나 키를 넘길 게 없습니다.

```
├── index.html
├── assets/
│   ├── vendor/leaflet/         Leaflet 1.9.4 (BSD-2, 동봉 — CDN 불필요)
│   ├── css/app.css
│   ├── js/
│   │   ├── config.js           타일 설정, 14개 시군, 학교급·농촌유학 분류
│   │   ├── labels.js           라벨 자동배치 엔진 (겹침 회피 + 지시선)
│   │   ├── edit.js             편집 모드 (폼·검증·내보내기)
│   │   └── app.js              메인
│   └── data/
│       ├── boundaries.js       14개 시군 경계 (16KB)
│       └── eupmyeon.js         읍·면·동 중심 좌표
└── data/regions/               ★ 시군별 학교 데이터 — 여기만 고치면 됩니다
    └── jeongju.js  gunsan.js  iksan.js  jeongeup.js  namwon.js  gimje.js
        wanju.js  jinan.js  muju.js  jangsu.js  imsil.js  sunchang.js
        gochang.js  buan.js
```

## 화면

- **지역 선택창** — 기본 `= 전북 전체 (742교) =`, 시부/군부로 묶인 14개 시군.
  시군을 고르면 바깥이 흐려지고 그 지역만 또렷하게 남습니다.
- **형태 선택창** — 초/중/고, 농촌유학 운영, 희망, 운영＋희망
- **검색** — 학교명·읍면동·주소
- **총 N교 / 학생 N명** — 현재 필터 기준 실시간 집계
- **카드 목록** — 학교급·농촌유학 태그, 주소, 전화, 학생수·학급수.
  클릭하면 지도가 그 학교로 이동하고 해당 마커가 강조됩니다.
  전북 전체에서는 시군별로, 시군을 고르면 읍·면·동별로 묶입니다.
- **라벨** — 기본 `자동`. 전북 전체에서는 농촌유학 학교만, 시군을 고르면 전부 표시합니다.
  줌·이동할 때마다 겹치지 않는 자리를 다시 계산하고, 자리가 없으면 라벨만 숨깁니다(확대하면 다시 나옵니다).
  선택한 학교의 라벨은 모드와 상관없이 항상 보입니다.

### 좁은 화면

목록이 지도 위로 올라오는 **바텀시트**가 됩니다. 위쪽 손잡이를 끌어 크기를 바꾸고,
톡 치면 세 단계(요약만 → 기본 → 목록 위주)를 돕니다.

- 목록에서 학교를 고르면 시트가 접히면서 지도가 크게 열립니다. 손잡이를 올리면
  보던 자리 그대로 목록으로 돌아옵니다.
- 시군을 고르거나 학교를 고를 때, 시트가 가리는 높이를 뺀 영역에 맞춰 지도가 움직입니다.
- 마커·라벨과 카드가 좁은 화면용 크기로 줄어들고, 헤더는 한 줄로 접힙니다.

## 데이터

| 시군 | 학교 | 초 | 중 | 고 | 학생수 | 농촌유학 운영 | 희망 |
|---|--:|--:|--:|--:|--:|--:|--:|
| 전주시 | 145 | 75 | 41 | 29 | 69,915 | 0 | 0 |
| 익산시 | 104 | 60 | 26 | 18 | 25,062 | 2 | 1 |
| 군산시 | 80 | 50 | 18 | 12 | 25,803 | 2 | 0 |
| 정읍시 | 65 | 33 | 19 | 13 | 8,457 | 4 | 1 |
| 김제시 | 55 | 32 | 13 | 10 | 5,838 | 1 | 0 |
| 완주군 | 51 | 30 | 13 | 8 | 8,288 | 2 | 2 |
| 남원시 | 48 | 26 | 13 | 9 | 6,249 | 2 | 0 |
| 고창군 | 40 | 20 | 14 | 6 | 4,164 | 2 | 1 |
| 부안군 | 37 | 18 | 12 | 7 | 3,261 | 2 | 2 |
| 진안군 | 28 | 13 | 10 | 5 | 1,507 | 9 | 0 |
| 순창군 | 25 | 15 | 7 | 3 | 1,900 | 11 | 2 |
| 임실군 | 25 | 14 | 8 | 3 | 1,324 | 7 | 1 |
| 장수군 | 21 | 9 | 7 | 5 | 1,360 | 3 | 0 |
| 무주군 | 18 | 9 | 5 | 4 | 1,523 | 1 | 1 |
| **합계** | **742** | 404 | 206 | 132 | **164,651** | **48** | **11** |

좌표는 742개교 전부 확정돼 있습니다.

### 출처와 가공 규칙

| 항목 | 출처 | 처리 |
|---|---|---|
| 학교명·소재지·주소·전화·학급수·학생수 | 2026학년도 전북교육청 초·중·고 현황 | 소계·합계 행 제외, 14개 시군만. 학생수는 특수학급 포함 **합계 전체** 열 |
| 위도·경도 | 2026 학교별 위치정보 | 학교명을 정규화(`초등학교→초` 등)해 매칭 |
| 농촌유학 | 희망학교·운영학교 지정 현황 | **2026-2학기 희망(P열)** 과 **운영(Q열)** 을 대조해, 두 곳에 다 있으면 **운영**, 희망에만 있으면 **희망** |

농촌유학 판별: 2학기 희망 45교 ∩ 운영 48교 = 33교 중복 → 운영 48 / 희망 12.
이 중 `덕유샘학교(중)` 은 초·중·고 현황에 없는 각종학교라 **제외했습니다**.
따라서 지도에 실린 것은 **운영 48 / 희망 11**입니다.

학기를 바꾸려면 같은 파일의 1학기 열(희망 N · 운영 O)로 다시 뽑으면 됩니다.
1학기 기준은 운영 44 / 희망 16 이었습니다.

**교차검증** — 별개 자료인 `전북 농어촌 유학생 명단`(2026-08-20)에서 종료일 칸이
`유학중` 인 재학생 350명의 소속 학교를 뽑으면 48곳이 나오는데, 위 지정 현황으로
정한 운영학교 48곳과 **정확히 일치**합니다(차이 0). 손으로 만들었던 순창 지도
(운영 11 · 희망 2)와 남원 지도(운영 2 · 희망 1)와도 맞습니다.

## 데이터 고치기

### 앱 안에서 (권장)

주소 뒤에 **`?edit=1`** 을 붙이면 편집 모드가 켜집니다.
예: `https://<사이트>/?edit=1` · 끄려면 `?edit=0` 또는 막대의 [편집 끝]

- **학교 상태 변경** — 학교를 고르면 상세카드에 `미지정 / 희망 / 운영` 버튼이 나옵니다. 한 번 눌러 바꿉니다.
- **정보 수정** — [정보 수정]으로 학교명·학교급·읍면동·학생수·학급수·주소·전화·좌표·메모를 고칩니다.
  좌표는 [지도에서 위치 찍기]를 누르고 지도를 클릭하면 채워집니다.
  유학 종료 같은 이력은 **메모**에 적어 두면 상세카드에 표시됩니다.
- **학교 추가 / 삭제** — 막대의 [+ 학교 추가], 수정 폼의 [삭제].

서버가 없으므로 편집 내용은 **이 브라우저에만** 쌓입니다(새로고침해도 남습니다).
확정하려면 **[내보내기]** 로 시군별 파일을 받아 저장소의 `data/regions/` 에 덮어쓰세요.
GitHub 웹에서 해당 파일을 열고 연필 아이콘 → 전체 선택 후 붙여넣기 → Commit 하면 됩니다.
[되돌리기]는 브라우저에 쌓인 편집분만 지우고 저장소 파일은 건드리지 않습니다.

> 편집분을 내보내기 전에 저장소 파일이 갱신되면, 묵은 편집분이 새 데이터를 덮어쓰지
> 않도록 자동으로 건너뛰고 알려 줍니다.

### 파일을 직접

`data/regions/<시군>.js` 한 파일만 고치면 됩니다. GitHub 웹에서 연필 아이콘으로 바로 편집해도 됩니다.

```js
{"n":"구림초등학교","t":"elem","ph":"구림면","rural":"운영",
 "lat":35.4527504,"lng":127.1009503,
 "addr":"순창군 구림면 방화로 6","tel":"063-652-8434","stu":19,"cls":6}
```

| 필드 | 뜻 |
|---|---|
| `n` `t` | 학교명 / `elem`·`mid`·`high` (필수) |
| `lat` `lng` | 좌표 (필수) |
| `ph` | 읍·면·동 — 목록 그룹 기준 |
| `rural` | `'운영'` \| `'희망'` — 없으면 일반 학교 |
| `stu` `cls` | 학생수 / 학급수 |
| `addr` `tel` | 주소 / 전화 |
| `branch` | 분교 |
| `approx` | 좌표 미확정 (점선 마커 + 경고 표시) |
| `ox` `oy` | 라벨 수동 오프셋(px). 겹치면 자동으로 비켜남 (`lock:true` 면 고정) |
| `dir` | 선호 방향 힌트 `r l t b tr tl br bl` |

새 좌표가 필요하면 [openstreetmap.org](https://www.openstreetmap.org) 에서 해당 위치를
우클릭 → "이 위치 보기" 하면 위도·경도가 나옵니다.

원본 순창 지도의 `ox/oy` 25건과 남원 지도의 8방향 배치 39건은 그대로 계승했습니다.

## 배경지도

화면 오른쪽 위 **배경** 선택창에서 바로 바꿔 볼 수 있고, 고른 값은 그 브라우저에 저장됩니다.
전부 API 키가 필요 없습니다.

| 값 | 배경 | 비고 |
|---|---|---|
| `plain` | 단순 (밝은 회색) — **기본** | CARTO Positron. 지명은 있고 색이 절제돼 마커가 잘 보입니다 |
| `nolabel` | 아주 단순 (지명 없음) | 도형과 도로만. 학교 라벨만 남기고 싶을 때 |
| `osm` | 기본 OSM (지명 많음) | 정보량이 많아 흑백 필터를 걸어 둡니다 |
| `dark` | 어두운 배경 | 발표 화면용 |
| `none` | 배경 없음 | 흰 바탕에 경계와 마커만. 인쇄·문서 삽입용 |

모두에게 적용할 기본값은 `assets/js/config.js` 의 `JB.TILE_DEFAULT` 를 고칩니다.
타일 제공자를 새로 넣으려면 같은 파일의 `JB.TILE_PRESETS` 에 항목을 추가하세요.

```js
JB.TILE_PRESETS.mine = {
  label: '내 타일',
  url: 'https://.../{z}/{x}/{y}.png',
  attribution: '&copy; 제공자',
  maxZoom: 19,
  gray: false        // true 면 흑백 필터
};
JB.TILE_DEFAULT = 'mine';
```

> OSM 공식 타일과 CARTO 무료 타일 모두 [사용](https://operations.osmfoundation.org/policies/tiles/)
> [정책](https://carto.com/attributions)이 있습니다. 트래픽이 크게 늘면 MapTiler 같은 유료
> 제공자로 옮기세요. 국내 지명이 촘촘한 배경이 필요하면 VWorld 도 같은 방식으로 붙습니다.

## 배포

빌드 과정이 없는 정적 파일입니다. 폴더째 올리면 끝입니다.

**GitHub Pages** — 저장소가 public 이어야 합니다(private 은 유료 플랜).
Settings → Pages → Source `Deploy from a branch`, Branch `main` / `/ (root)` → Save.
Jekyll 처리를 끄려고 `.nojekyll` 을 넣어 뒀습니다.

**로컬에서 보기** — 폴더 구조를 그대로 두고 `index.html` 을 더블클릭하면 됩니다.
브라우저가 로컬 파일을 막으면 폴더에서 `python -m http.server 8000` 을 띄우고
`http://localhost:8000` 으로 여세요.

## 경계 데이터

[southkorea/southkorea-maps](https://github.com/southkorea/southkorea-maps) `kostat/2018` 시군구 경계에서
전북 14개 시군만 뽑아 Douglas–Peucker(≈120m)로 단순화한 뒤 폴리라인으로 인코딩했습니다(18MB → 16KB).
MultiPolygon 을 전부 처리해 군산·부안의 섬도 나옵니다. 전주시는 완산구·덕진구를 합쳐 한 항목으로 넣었습니다.
